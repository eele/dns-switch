# DNS Switch 开发计划

Windows 下按网卡切换 DNS 地址的小工具（全英文界面 + 系统托盘）。**技术栈：Tauri 2（Rust 后端 + 系统 WebView2 渲染 + Svelte 5 / TypeScript 前端）**，界面**完整复刻经典 Mac OS X Aqua 窗口外观**（Panther/Tiger 时期，2003–2007）：无边框 + 自绘金属质感标题栏（糖果球红绿灯按钮）+ 8px 圆角 + 银色渐变标题栏 + 浅灰底白色卡片行 + Aqua 蓝色强调，观感与 Mac OS X 原生窗口一致（详见 3.4）。设计参考：`macos/` 目录下三张经典 iCal / Address Book 截图。

> **本文件已于 Tauri 迁移后同步重写**（原方案为 Electron，迁移记录见文末「附：迁移说明」）。当前实现状态速览见第 10 节。

## 1. 目标与功能

- 选择一张网卡，将其 DNS 切换到指定的一组主/备 DNS 地址
- GUI 全英文界面，**完整复刻经典 Mac OS X Aqua 窗口外观**：无边框窗口 + 自绘金属银色标题栏（左上角红黄绿糖果球红绿灯按钮、居中标题）+ 浅灰（#ECECEC）窗口主体 + 白色圆角卡片行；8 px 圆角窗体，整体观感与 Mac OS X Panther/Tiger 原生窗口完全一致（详见 3.4）
- 系统托盘图标：点关闭按钮最小化到托盘；托盘图标菜单含 **Open**（打开界面）和 **Exit**（退出程序）两个菜单项

界面布局（自上而下）：

```
┌──────────────────────────────────────────────────┐
│ 🔴 🟡 🟢              DNS Switch                    │  ← 标题栏（金属银渐变）
├──────────────────────────────────────────────────┤
│  Adapter:  [ WLAN                       v ] [Refresh] │
│  Current DNS:  8.8.8.8  /  8.8.4.4              │
├──────────────────────────────────────────────────┤
│  (o) AliDNS  223.5.5.5  223.6.6.6  [Edit] [Del] │
│  ( ) Google DNS  8.8.8.8  8.8.4.4  [Edit] [Del] │
│  ( ) DNSPod  1.12.12.12  1.12.0.0  [Edit] [Del] │
│  ( ) Automatic (DHCP)  Automatic  —             │
├──────────────────────────────────────────────────┤
│  [ Add ]                                        │
│  Status: DNS on "WLAN" updated successfully.    │
└──────────────────────────────────────────────────┘
```

交互规则：

- **启动恢复上次网卡**：界面打开时优先选中上次切换/选择的网卡（`last_adapter`），而不是默认选第一个；若该网卡已不存在或状态非 Up，回退选中列表第一个网卡；每次切换网卡后将所选写回 `last_adapter`
- **Current DNS 显示行**：位于网卡选择与 DNS 组列表之间，展示当前选中网卡的当前主/备 DNS 地址；**切换网卡时自动触发重新获取**（点 Refresh 时一并刷新）；获取中显示 "Loading…"，若为自动/DHCP 或无地址则显示 "Automatic (DHCP)"
- 每行 = 一个 DNS 组：左侧单选框 + 组名 + 主 DNS + 备 DNS + 右侧 **Edit/Save 切换按钮**，其右边紧跟 **Delete 按钮**
- **点选单选框即生效**：立即把该组的主/备 DNS 应用到上方选中的网卡，状态栏提示结果；应用成功后同步刷新 Current DNS 显示行
- **单选选中态反映网卡真实当前 DNS**：加载/切换网卡时按该网卡**实际当前 DNS** 决定勾选——为自动/DHCP 时勾选 Automatic (DHCP) 行；当前主/备 DNS 与某组完全一致时勾选该组；**若不属于本工具 DNS 列表（如在本工具之外被手动改过），所有单选框保持全不选**（此时不自动应用任何组）
- 点 **Edit**：该行的**组名**和**主/备地址**均变为可编辑输入框，按钮变为 **Save**；Save 时校验 IP 合法性与**组名唯一性**（不能为空、不能与其他组重复、不能用保留名），全部通过才写回配置并恢复只读，未通过则状态栏提示且不保存
- **Add**：新增一组 DNS（组名可编辑，主/备默认空），加入列表并进入编辑态
- **Delete**：每个 DNS 组行右侧（Edit 按钮右边）各有一个 Delete 按钮，点击删除**该组**；弹框二次确认（`Yes/No`）后才删除；Automatic (DHCP) 行没有 Delete 按钮
- **Automatic (DHCP) 固定行**：列表末尾恒定追加一行 "Automatic (DHCP)"（不写入配置）；点选它即把当前网卡恢复为 DHCP 自动获取；该行**既无 Edit 也无 Delete 按钮（不可编辑、不可删除）**；组名 "Automatic (DHCP)" 为保留名，Add/Edit 时重名被拒绝
- **DNS 地址选择与复制**：界面展示的所有 DNS 地址（**Current DNS 行**、**每组行的主/备地址**）均支持**鼠标拖拽选择**（可整段选中或只选其中一部分）；在地址上**右键**弹出上下文菜单，单项 **Copy**（有选区时复制选中部分，无选区时复制该单元格整段地址），复制结果写入系统剪贴板并提示 "Copied <ip>"；**Ctrl+C** 亦可复制选中部分（实现见 3.4）
- 点窗口**红灯（关闭）**按钮 **不退出**，最小化到托盘；托盘菜单 **Open** 恢复窗口，**Exit** 真正退出
- **绿灯**最大化/还原窗口；**黄灯**最小化到任务栏；双击标题栏 = 绿灯功能

## 2. 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 框架 | **Tauri 2**（Rust 后端 + 系统 WebView2 渲染前端） | 单 exe 约 8–9 MB（对比 Electron 80–150 MB）；后端为编译型 Rust 进程，内存占用低；`#[tauri::command]` 直接把 Rust 函数暴露给前端 `invoke()`，天然 async、不阻塞 UI；MIT 许可 |
| 前端 | **Vite + Svelte 5 + TypeScript** | 编译型框架、无虚拟 DOM、运行时仅几 KB；`$state` 响应式天然适配"行内 Edit/Save 切换"；Vite HMR 让 UI 改动即改即见；产物是纯静态文件，`frontendDist: ../dist` 直接嵌入 |
| 渲染 | **WebView2**（Windows 系统自带 Chromium 内核） | 圆角/渐变/`box-shadow`/文本选择等 Aqua 观感所需 CSS 能力与 Chromium 一致；无需自带浏览器内核，这是体积优势的来源；Chromium 系故 `navigator.clipboard`、`user-select`、`window.getSelection()` 均为原生能力 |
| 托盘 | **Tauri `tray-icon` feature**（`TrayIconBuilder` + `Menu`） | 官方一等支持：`Menu::with_items` + `on_menu_request` 自绘右键菜单，`on_tray_icon_event` 处理左键显示窗口；与窗口事件（`CloseRequested`）在同一 Rust 事件循环内联动，无跨线程 UI 问题 |
| 提权 | **Rust + Win32 FFI 自实现**（`IsUserAnAdmin` 检测 + `ShellExecuteW("runas")` 自提升） | 无第三方 crate 依赖，逻辑约 100 行；比 `is-elevated` 之类 Node 包更直接可控（见 3.2） |
| DNS 操作 | Rust `std::process::Command` 调系统内置 **PowerShell**（`NetAdapter` / `DnsClient` 模块） | 系统自带、无需第三方驱动或 windows crate；`ConvertTo-Json` 输出用 `serde_json` 解析最省事；`CREATE_NO_WINDOW` 创建标志避免闪黑框（见 3.1） |
| 配置存储 | 本地 **JSON** 文件（exe 同级，`serde_json`） | 简单、可手改、便于便携；原子写（临时文件 + rename） |
| 构建/出包 | **`@tauri-apps/cli`**（`npm run package:win` = `tauri build`） | 一条命令完成前端构建 + Rust release 编译 + 打包；同时产出 NSIS/MSI 安装包与可拷贝的单 exe |
| 测试 | **Playwright 两层**：① 纯浏览器 + mock API ② **CDP 驱动打包后的真实 exe** | 应用要求管理员权限，纯浏览器层免提权跑通全部前端逻辑；CDP 层直接 attach 打包产物的 WebView2 远程调试端口，做真机端到端断言（见 7.2） |

依赖汇总：运行期仅系统 WebView2（Windows 10/11 默认已装）+ 约 9 MB 单 exe；前端构建期 Vite/Svelte/TypeScript 工具链；Rust 侧仅 `tauri`（`tray-icon`）+ `serde`/`serde_json`，Windows 目标额外 `raw-window-handle`（用于取 HWND 调 DWM）。开发期需 **Rust 工具链（rustc/cargo）+ Node.js LTS + Microsoft C++ Build Tools（MSVC）**，均为一次性安装。

> 备选对比（含**本项目实际迁移经历**）：
> - **Electron（原方案，已弃用）**：`Tray`/`BrowserWindow`/`ipcMain.handle` API 成熟，Playwright 有官方 Electron 驱动，前端逻辑零改动即可复用——这些正是当初选它的理由。**弃用原因**：运行时要捆绑 Chromium + Node（80–150 MB），而本项目全部后端逻辑只是「调 PowerShell + 读写一个 JSON + 托盘 + UAC」，Chromium 带来的体积/内存收益为零；迁移到 Tauri 后 exe 约 9 MB，且 E2E 改用「打包产物 + CDP」路径后同样能驱动真实窗口（见 7.2），并未丢失端到端调试能力。
> - **Wails v3（Go + WebView2）**：体积相当，托盘是 v3 头等特性；但 v3 曾长期处于 Beta，且存在自 alpha.22 起 `Hidden: true` 窗口选项在 Windows 失效的已知 bug（wails #4498，与"隐藏到托盘"场景直接相关）；E2E 无官方方案。
> - **egui / eframe（Rust 纯原生）**：单文件 5–15 MB、启动极快；但**无内置托盘**（需自行集成 `tray-icon` 并手工处理事件循环，egui #737/#7401），immediate-mode 下 Aqua 卡片/文本选择复制需大量自绘，且无标准 E2E 通道——"本地完整端到端界面调试"最难满足。
> - **Avalonia 11（C#/.NET）**：表现力强，但自包含发布 50–100+ MB；托盘靠第三方（H.NotifyIcon）；E2E 需自组 UIA/FlaUI 方案。
> - **Flutter Desktop**：引擎 60 MB+，Material 风格与 Aqua 观感路线不符，托盘与 E2E 均靠社区包。
> - **WinUI 3 / Windows App SDK**：原生观感最佳，但 MSIX 打包繁琐，托盘/E2E 生态同样不全。
>
> 综上，在"WebView 混合方案"这一档里，**Tauri 2** 相对 Electron 的核心优势是体积与内存，相对 Wails 的优势是稳定版 + 更成熟的托盘/菜单 API；Aqua 观感所需的 CSS 自由度由 WebView2 保证，因此是当前最优解。

## 3. 核心实现要点

### 3.0 工程结构（Tauri：Rust 主进程 + WebView2 前端）

- **进程模型**：Rust 侧持有事件循环、窗口、托盘与全部命令处理（`src-tauri/src/main.rs` 的 `invoke_handler`）；前端在 WebView2 中运行 Svelte
- **前后端通信**：前端 `invoke("cmd", { args })` → Rust `#[tauri::command] fn cmd(...)`（返回 `Result<T, String>`，错误自动 reject 成 JS 异常）；命令同步执行但由 Tauri 派发到工作线程，**不阻塞 UI**——前端 `await` 后直接更新状态栏/单选态
- **窗口**：标题 "DNS Switch"，初始 628×528（`minWidth: 468 / minHeight: 408`），居中；**`decorations: false`（无边框）+ `transparent: true` + `shadow: false`**，由前端自绘 Aqua 标题栏与窗体圆角/投影（详见 3.4）
- **本地调试闭环**：`npm run tauri:dev`（自动拉起 Vite dev server + Rust 热重载）跑真实窗口 + HMR；`npm run dev` 纯浏览器模式（`api.ts` 自动降级为 mock，见 3.7）做免提权快速迭代；WebView2 支持 F12 DevTools 调试前端；再叠加第 7 节两层 Playwright，覆盖"本地完整端到端界面操作调试"

```rust
// src-tauri/src/commands.rs —— 命令层示意
#[tauri::command]
pub fn set_dns(adapter_index: u32, servers: Vec<String>) -> Result<(), String> {
    let list = servers.iter().map(|s| format!("'{}'", s)).collect::<Vec<_>>().join(",");
    run_ps(&format!("Set-DnsClientServerAddress -InterfaceIndex {} -ServerAddresses ({})",
                    adapter_index, list)).map(|_| ())
}
```

```ts
// src/api.ts —— 前端调用（单一入口，浏览器下自动降级 mock）
import { invoke } from "@tauri-apps/api/core";
export async function setDns(adapterIndex: number, servers: string[]): Promise<void> {
  if (isTauri()) { await invoke("set_dns", { adapterIndex, servers }); return; }
  /* 浏览器 mock 分支：改 MOCK_CURRENT_DNS，供 E2E 使用 */
}
```

> IPC 参数命名约定：Rust 侧 snake_case 形参（`adapter_index`）在前端 `invoke` 时须写 **camelCase**（`adapterIndex`），Tauri 2 默认按 camelCase 匹配；`AppConfig` 结构体因带 `#[serde(rename_all)]` 语义与磁盘 JSON 一致，故类型定义保持 `snake_case`。

### 3.1 DNS 操作（`src-tauri/src/commands.rs`，`std::process::Command`）

统一封装 `run_ps(script) -> Result<String, String>`：`powershell -NoProfile -Command <script>`，Windows 下加 `CREATE_NO_WINDOW`（`0x0800_0000`）创建标志隐藏控制台窗口；非 0 返回码 → 抛错并带上 **stderr 原文 + 命令 stdout**（前端状态栏原样展示）。`ConvertTo-Json` 输出用 `parse_json_array()` 规范化（**单元素时不包数组**的坑已处理）。

| 功能 | Rust 命令 | PowerShell |
|---|---|---|
| 列出可用网卡 | `list_adapters` | `Get-NetAdapter \| Where-Object Status -eq 'Up' \| Select-Object Name,InterfaceIndex \| ConvertTo-Json` |
| 读取网卡当前 DNS + 是否 DHCP | `get_current_dns` | `Get-DnsClientServerAddress -InterfaceIndex <n> -AddressFamily IPv4` + 注册表 `NameServer` 判定（见下） |
| 设置 DNS | `set_dns` | `Set-DnsClientServerAddress -InterfaceIndex <n> -ServerAddresses ('<primary>','<secondary>')` |
| 恢复自动(DHCP) | `reset_dns_to_dhcp` | `Set-DnsClientServerAddress -InterfaceIndex <n> -Reset` |

- 只列出 `Status -eq 'Up'` 的网卡，过滤已禁用/未连接网卡；后续可再加 VPN/虚拟网卡名称黑名单
- **DHCP 判定的正确姿势（关键坑）**：`Get-DnsClientServerAddress` 在 DHCP 自动获取时**同样返回有效地址**，"地址列表为空"不能当作 DHCP 判据。真实判据是注册表
  `HKLM\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters\Interfaces\{InterfaceGuid}` 下的 `NameServer` 值——**存在且非空即静态**，否则为 DHCP。同一条 PowerShell 脚本内一并取该 GUID（直接来自 `Get-NetAdapter` 的 `InterfaceGuid`，避开按名字/IP 反查的匹配坑），返回 `{ servers, isDhcp }` 压缩 JSON 给 Rust 解析
- 前端据此得到 `DnsInfo { primary, secondary, isDhcp }`，`isDhcp` 决定勾选 Automatic (DHCP) 行
- **提速备选**（未实现）：`netsh interface ip set dns "WLAN" static 8.8.8.8 primary` 等，省掉 PowerShell 冷启动（约 0.5~1 s），但输出需手工解析；v1 先保 PowerShell 求稳

### 3.2 管理员权限（Rust + Win32 FFI，启动时检测 + 自提升）

修改网卡 DNS 必须管理员权限。实现于 `src-tauri/src/admin.rs`：

- **启动检测**：`main.rs` 最前面调用 `ensure_elevated()`（在 `Builder::new()` 之前）：`shell32!IsUserAnAdmin()` 判当前进程
  - 已提权 → 正常建窗口
  - 未提权 → `GetModuleFileNameW(NULL)` 取当前 exe 路径，`shell32!ShellExecuteW(hwnd, "runas", exe, NULL, NULL, SW_SHOWNORMAL)` 触发 UAC 自提升，成功则 `std::process::exit(0)` 退出当前（非提权）进程，避免两个实例
  - 用户在 UAC 选"否"（返回 `SEERR_UAC_CANCEL` 等）→ `user32!MessageBoxW` 提示需管理员权限后 `exit(0)`，不创建任何窗口、无残留进程
- **测试逃生门**：环境变量 `DNS_SWITCH_ALLOW_NO_ADMIN=1` 时跳过提权直接建窗——这是 7.2 真机 E2E 能在普通终端跑起来的前提（写类断言在无提权时优雅失败/跳过）
- **开发态**：从**管理员终端**跑 `npm run tauri:dev` 即可（终端提权一次，子进程全部继承，不触发自我提升）

### 3.3 系统托盘（Tauri `tray-icon`）

- 图标：`TrayIconBuilder::with_id("main-tray").icon(app.default_window_icon())`，复用打包进 binary 的应用图标（`src-tauri/icons/`，由 `scripts/generate-icon.mjs` 从 `scripts/icon-source.png` 生成全套 PNG/ICO/ICNS），无需运行期读文件
- **左键点击** → `on_tray_icon_event`（`TrayIconEvent` 命中 + `MouseButton::Left` + `MouseSlot::Up`）→ `window.show()` + `set_focus()`
- **菜单**（`Menu::with_items` + `on_menu_request` → `TrayIcon::popup_menu`）：**Open** → `show + set_focus`；**Exit** → `app.exit(0)`
- **关闭→入托盘**：`WebviewWindowBuilder::on_window_event` 拦截 `WindowEvent::CloseRequested { api, .. } → api.prevent_close() + window.hide()`；此后窗口常驻隐藏，靠托盘 Open/Exit 控制（进程不退出，退出只走托盘 Exit 或红绿灯红灯→同样入托盘）

### 3.4 GUI（Svelte 5 runes + CSS，Aqua 复刻 + Win10/11 外观一致）

- 主题基调：**固定浅色**（CSS 即真理，无系统深色跟随问题）；全部色值/圆角/字体集中在 `src/theme.css` 的 `:root` CSS 变量（`--bg-window #ECECEC`、`--bg-card #FFF`、`--text-primary #1D1D1F`、`--border #9D9D9D`、`--accent #3876D6`、`--success #28C840`、`--error #FF3B30` 等），组件一律引用变量不写死
- 设计基调（**经典 Mac OS X Aqua，Panther/Tiger 时期**，参考 `macos/` 下 iCal / Address Book 截图）：
  - 卡片：`border-radius: 6px` + 1px `#C9C9C9` 描边 + `box-shadow: 0 1px 2px rgba(0,0,0,.12)`；按钮/输入框/下拉 `border-radius: 5px`；窗口整体 `border-radius: 8px`
  - 字体：`"Lucida Grande", -apple-system, "Segoe UI", "Helvetica Neue", "Microsoft YaHei", sans-serif`；标题 13px/600、正文 13px、辅助 11px
  - 强调/状态色：主按钮（Add/Refresh）Aqua 蓝四段 glossy 渐变（`#9CC7FF→#5B9BE8→#3876D6→#1E5FC0`）；次要按钮白→浅灰 two-tone；状态栏成功绿/失败红/进行中灰按 class 切换；过渡 `transition: ... .1s ease`
- **自绘标题栏**（`src/components/TitleBar.svelte`，无边框窗口）：
  - 容器 `data-tauri-drag-region` 提供拖拽（**Tauri 属性，非 Electron 的 `-webkit-app-region`**）；按钮设 `data-tauri-drag-region="false"` 防误触
  - **红绿灯三按钮**（左→右：关闭 / 最小化 / 最大化）：直径 13 px、间距 8 px、距左 13 px，垂直居中于 32 px 标题栏；糖果球用径向渐变 + inset 高光（红 `#FF9D94→#FF5F56→#E5433A`、黄 `#FFE28C→#FFBD2E→#E6A01F`、绿 `#8FE9A0→#28C840→#1EAE35`）；hover 该组时同时显示三个符号（`× / − / ⊞`，SVG opacity 0→1）
  - **按钮行为**（走 Tauri window API，无需自定义 IPC）：红 → `getCurrentWindow().close()`（被 3.3 的 `CloseRequested` 拦截成"入托盘"）；黄 → `getCurrentWindow().minimize()`；绿 → `getCurrentWindow().toggleMaximize()`
  - **标题文字**水平居中，13px/600/`#4D4D4D`；标题栏金属银纵向渐变 + 顶部白色 inset 高光 + 底部 1px `#9D9D9D` 分隔线
  - **双击标题栏**：监听 `dblclick` → `toggleMaximize()`（Aqua zoom 行为）
- **窗口圆角 / 投影 / Win10-11 一致（本项目的核心难点）**：
  1. 配置层：`decorations: false` + `transparent: true` + `shadow: false` —— **关掉 DWM 自带的非客户区圆角与投影**，否则 Win11 会自动给无边框窗口套 8 px 圆角，而 Win10 没有，两系统观感不一致
  2. CSS 层：`#root` 用 `inset: var(--window-margin)`（24 px）从透明窗口边缘内缩，再设 `border-radius: 8px` + `box-shadow: var(--window-shadow)`（`0 6px 18px rgba(0,0,0,.35)` + 1 px 描边）自绘窗体与阴影；`html/body` 背景保持透明
  3. 原生兜底（`main.rs` 的 `setup` 钩子）：通过 `raw-window-handle` 取 `hwnd`，调 `dwmapi!DwmSetWindowAttribute(hwnd, DWMWA_WINDOW_CORNER_PREFERENCE /* 33 */, DWMWCP_DONOTROUND /* 1 */)`，把 DWM 圆角显式设为"不圆"，保证 Win11 上不残留双圆角/白边
  4. 阴影/投影不依赖 `windowEffects`（`EffectsBuilder` 在该分支不可用），纯 CSS 实现
- 布局与交互同第 1 节：顶部网卡下拉 + Refresh；中部 "Current DNS" 行 + 动态 DNS 组行（`components/DnsRow.svelte`）+ 末尾固定 Automatic (DHCP) 行（无 Edit/Delete）；底部 **Aqua 工具栏条**（浅灰渐变 + 顶部 1 px 分隔线，含 Add + 状态栏）
- **各列左对齐**：CSS Grid 固定列模板（单选/组名/主 DNS/备 DNS/Edit/Delete 各一列），组名列定宽 → 组名长短不一时主/备/按钮仍纵向对齐；组多时外层 `overflow-y: auto`（Aqua 风格细滚动条）
- **Edit 态**：每行持 `editing` 状态（Svelte 5 `$state`），为真时组名/主/备渲染 `<input>`（Aqua 内凹阴影输入框），按钮文案切 Save；校验通过才写回配置并退出编辑态
- **删除二次确认**：自绘 Aqua 对话框（`components/ConfirmDialog.svelte`，No/Yes，Yes 为 Aqua 蓝默认按钮）
- **DNS 地址选择与复制**（WebView2 = Chromium，均为原生能力）：
  - 地址单元格用 `<span class="addr">` + `user-select: text`：**拖拽选择、Ctrl+C 复制选区为浏览器原生行为**；单元格非输入框，天然"只读不可键入"
  - **右键菜单**（`components/ContextMenu.svelte`）：监听 `contextmenu` → 阻止默认 → 自绘 Aqua 小菜单（当前一项 **Copy**）；Copy 取 `window.getSelection().toString()`，无选区则复制该单元格整段地址；经 `navigator.clipboard.writeText()` 写系统剪贴板，状态栏提示 "Copied <ip>"
  - Automatic 行的 "Automatic" 占位非真实地址，`user-select: none`

### 3.5 数据模型与持久化（`config.json`，exe 同级）

```json
{
  "dns_groups": [
    {"name": "AliDNS", "primary": "223.5.5.5", "secondary": "223.6.6.6"},
    {"name": "Google DNS", "primary": "8.8.8.8", "secondary": "8.8.4.4"}
  ],
  "last_adapter": "WLAN",
  "selected_group": "AliDNS"
}
```

- 位置：`config_path()`——`std::env::current_exe()` 的父目录（**打包后与 exe 同级，便携、可手改**）；取不到 exe 路径时（开发态异常）回落到当前工作目录（项目根）
- 读写：Rust 侧 `load_config()` / `save_config(&cfg)` 两个命令，`serde_json` 序列化；加载失败/字段缺失/文件损坏 → 回落默认 3 组（AliDNS / Google DNS / DNSPod），**绝不 panic**
- **原子写**：先写同目录 `config.json.tmp` 再 `fs::rename` 覆盖，避免中途断电/杀进程写坏
- `selected_group` 记录上次勾选组名，可为 `"Automatic (DHCP)"`，**也可为空字符串（全不选）**；但加载/切换网卡时勾选态**不以此值为准，按网卡实际当前 DNS 重新计算**
- `last_adapter` 记录上次选中/切换的网卡名，**每次切换网卡即写回**（`onAdapterChange → persistConfig`）；对应网卡已消失/非 Up 时忽略该值

### 3.6 校验与异常

- IPv4 用 `isValidIPv4(ip)` 校验（`src/api.ts` 导出，前端即时提示 + 后端命令本身失败也回传 stderr 原文）
- 组名校验统一应用于 **Edit→Save** 与 **Add** 两入口：trim 后非空、与其他组名不重复（大小写不敏感）、不得为保留名 `"Automatic (DHCP)"`；违反则状态栏提示且不落盘
- 删除后若选中项被删 → 自动选中第一组（列表为空则清空选中）
- DNS 切换为 async 调用（不阻塞渲染），状态栏 "Applying …" → 结果；失败红色提示且**不改变单选状态**
- 校验函数（IPv4 / 重名 / 保留名）逻辑单一来源，前端两处入口共用，避免 Add/Edit 行为漂移

### 3.7 前端 API 适配层与浏览器 mock（`src/api.ts`）

`isTauri()`（检测 `window.__TAURI_INTERNALS__`）决定每个函数走 `invoke` 还是内存 mock：

- mock 网卡 3 张（WLAN / Ethernet / Bluetooth Network），`MOCK_CURRENT_DNS` 随 `setDns`/`resetDnsToDhcp` 变更 → 可在**普通浏览器**里跑完整交互链路
- `loadConfig`/`saveConfig` 在浏览器下用 `localStorage` 模拟落盘（含损坏回退默认值分支）
- 意义：前端逻辑迭代与 E2E（7.1）**不需要管理员权限、不需要 Rust 编译**；同时保证浏览器与 Tauri 两套运行方式使用同一份 UI 代码，测试断言对真机同样有效

## 4. 代码结构

```
dns_switch/
├── package.json                # scripts: dev/build/preview | tauri:dev | tauri:build | package:win | test:e2e | typecheck
├── vite.config.ts              # base: "./"（打包产物内嵌资源需相对路径）+ svelte 插件 + 5173 strictPort
├── index.html                  # Vite 入口 HTML
├── src/                        # 前端（Svelte 5 runes + TypeScript）
│   ├── main.ts                 # 挂载入口
│   ├── App.svelte              # 主界面：初始化/网卡选择/Current DNS/组列表/校验/剪贴板/状态栏（~460 行）
│   ├── components/
│   │   ├── TitleBar.svelte     # Aqua 金属银标题栏 + 红绿灯（Tauri window API + data-tauri-drag-region）
│   │   ├── DnsRow.svelte       # 单组行：radio/组名/主备地址/Edit-Save/Delete，编辑态输入框
│   │   ├── ConfirmDialog.svelte# Aqua 风格 Yes/No 二次确认
│   │   └── ContextMenu.svelte  # 自绘右键 Copy 菜单
│   ├── api.ts                  # invoke 封装 + 浏览器 mock 降级 + isValidIPv4/isDuplicateName
│   ├── types.ts                # Adapter / DnsInfo / DnsGroup / AppConfig
│   ├── theme.css               # Aqua 色板（:root 变量）/ 窗体圆角+投影 / 红绿灯 / glossy 按钮（~490 行）
│   └── vite-env.d.ts
├── src-tauri/                  # Rust 后端
│   ├── Cargo.toml              # tauri(features=["tray-icon"]) + serde/serde_json + raw-window-handle(win)
│   ├── tauri.conf.json         # 窗口 628×528 / decorations:false / transparent:true / shadow:false
│   ├── build.rs                # tauri-build
│   ├── icons/                  # 32/128/@2x PNG、icon.ico、icon.icns、Store logos
│   └── src/
│       ├── main.rs             # ensure_elevated → setup(DWM 圆角) → invoke_handler → tray 菜单 → CloseRequested 入托盘
│       ├── commands.rs         # list_adapters / get_current_dns / set_dns / reset_dns_to_dhcp（run_ps + parse_json_array）
│       ├── config.rs           # AppConfig + load/save_config（exe 同级 JSON + 原子写 + 默认值）
│       ├── admin.rs            # IsUserAnAdmin / ShellExecuteW(runas) / MessageBoxW + DNS_SWITCH_ALLOW_NO_ADMIN
│       └── lib.rs
├── scripts/
│   ├── generate-icon.mjs       # icon-source.png → src-tauri/icons 全套尺寸 + .ico/.icns
│   └── icon-source.png         # 圆底 + 字母 "D" 源图
├── tests/
│   ├── playwright.config.ts    # testDir ./e2e + webServer: npx vite --port 5173（复用已起服务）
│   └── e2e/
│       ├── e2e.spec.ts         # 7.1 浏览器 + mock（18 个用例）
│       ├── exe-e2e.spec.ts     # 7.2 打包 exe + CDP(9333) 真机（20 个用例）
│       └── exe-cdp-test.mjs    # 7.2 的独立手工调试脚本（免 Playwright 直接连 CDP 看 DOM）
├── dist/                       # vite build 产物（tauri frontendDist）
├── release/                    # 出包目标位置（单 exe + 便携目录）
└── config.json                 # 运行期生成（exe 同级；开发态在项目根）
```

## 5. 开发步骤（✅ = 已完成）

1. ✅ **工具链**（一次性）：Node.js LTS + npm；`npm create vite@latest`（Svelte-TypeScript 模板）；`npm i -D @tauri-apps/cli` + Rust 工具链 + MSVC Build Tools；`npm run tauri:dev` 跑通空窗口（居中、无边框）
2. ✅ **`src-tauri/src/commands.rs`**：`run_ps`（`-NoProfile -Command` + `CREATE_NO_WINDOW` + 非 0 码带 stderr 抛错）+ 4 个命令 + `parse_json_array` 单元素规范化 + 注册表 `NameServer` 判 DHCP
3. ✅ **配置持久化**（`config.rs`）：exe 同级 JSON + 默认 3 组 + 损坏/缺失回退 + 原子写（`.tmp` + rename）+ `last_adapter`/`selected_group`
4. ✅ **Aqua 标题栏**（`TitleBar.svelte`）：13 px 糖果球红绿灯（径向渐变 + inset 高光，hover 出 ×/−/⊞）+ 居中标题 + 金属银渐变 + `data-tauri-drag-region` 拖拽 + 双击 toggleMaximize + `getCurrentWindow()` 三操作
5. ✅ **无边框窗体外观**：`decorations/transparent/shadow` 配置组合 + CSS `inset:24px` 内缩自绘圆角与投影 + Rust `DwmSetWindowAttribute(DWMWCP_DONOTROUND)` 兜底 → **Win10 与 Win11 观感完全一致**
6. ✅ **UI 骨架**：网卡下拉 + Refresh、Current DNS 行、动态组行（`DnsRow`）、固定 Automatic (DHCP) 行、底部 Aqua 工具栏（Add + 状态栏）、Grid 列对齐、`theme.css` 变量化色板
7. ✅ **地址选择与复制**：`user-select: text` 单元格 + `contextmenu` 自绘 Copy 菜单 + `navigator.clipboard.writeText()` + Ctrl+C 原生
8. ✅ **交互逻辑**：点单选即切换（"Applying…" → 结果）、Edit/Save 校验（IPv4 + 重名 + 保留名）、Add 进编辑态、Delete 二次确认（自绘对话框）、按真实当前 DNS 反推勾选（含"列表外全不选"）、启动恢复 `last_adapter`
9. ✅ **托盘 + 提权**：`TrayIconBuilder` + Open/Exit 菜单 + 左键显示；`CloseRequested → prevent_close + hide`；`ensure_elevated()`（UAC 自提升 / 拒绝则提示后退出）+ 测试逃生门
10. ✅ **测试**：`tests/` Playwright 两层（7.1 浏览器 mock 18 例 + 7.2 打包 exe CDP 20 例），`npm run test:e2e`；第 6 节可自动化项全绿
11. ✅ **打包**：`npm run package:win`（= `tauri build`）→ NSIS/MSI 安装包 + 可拷贝单 exe（约 8.9 MB）；便携产物已置于 `release/`
12. ✅ **迁移收尾**：移除 Electron 依赖与 `electron/` 目录，`README.md` 与本文同步为 Tauri 版

## 6. 验收清单

**功能**

- [ ] 网卡下拉列出所有 Up 状态网卡，Refresh 可刷新
- [ ] 启动时自动恢复上次选中的网卡（`last_adapter`）；切换后重启仍恢复；该网卡不存在或非 Up 时回退到列表第一个
- [ ] 切换网卡后 Current DNS 行自动重新获取并显示该网卡当前主/备 DNS（DHCP 时显示 "Automatic (DHCP)"）
- [ ] **DHCP 判定准确**：网卡为自动获取时即使 `Get-DnsClientServerAddress` 返回了地址，仍勾选 Automatic (DHCP) 行（注册表 `NameServer` 判据）
- [ ] 网卡当前 DNS 不在本工具 DNS 列表（如外部手动改为列表外地址）时所有单选框全不选；与某组主/备完全一致时勾选该组
- [ ] DNS 列表各列（单选/组名/主/备/Edit/Delete）左对齐，组名长短不一时后续列仍纵向对齐
- [ ] 点选任一 DNS 组后，`ipconfig /all` 验证该网卡 DNS 已变更为主/备地址，Current DNS 行同步更新
- [ ] 界面任一 DNS 地址均可鼠标拖拽部分选中；且该单元格不可键入（非输入框）
- [ ] 地址上右键出现 Copy 菜单：有选区 → 复制选中部分；无选区 → 复制整段地址；**Ctrl+C** 亦可复制选中部分；状态栏提示 "Copied <ip>"
- [ ] Edit → 改地址/组名 → Save：校验生效、持久化到 `config.json`、重启后保留
- [ ] Add 新增组成功并进入编辑态；组名重复（Add/Edit）或非法 IP 被拒绝并有提示
- [ ] 每行 Delete（Edit 右边）弹 Yes/No 二次确认，确认后该组删除且配置同步；取消则保留
- [ ] 列表末尾恒有 Automatic (DHCP) 行：无 Edit/Delete；选中它后 `ipconfig /all` 验证恢复 DHCP；使用保留名 "Automatic (DHCP)" 被拒绝
- [ ] 删除当前选中组后自动选中第一组；config 损坏/缺失时不崩溃并用默认数据

**外观 / 窗口**

- [ ] 全英文界面 + **完整 Mac OS X Aqua 风格窗口**：无边框 + 自绘金属银标题栏（13 px 糖果球红绿灯 ×3，hover 出 ×/−/⊞）；标题居中 13px/600/`#4D4D4D`；标题栏金属银渐变 + 1 px 底部分隔线；浅灰底 `#ECECEC` + 白色卡片（6 px 圆角 + 柔和投影）；Aqua 蓝 `#3876D6` 强调、状态色绿 `#28C840`/红 `#FF3B30`；色板全部取自 `theme.css` 变量，无深色残留、不随系统切深色
- [ ] 红绿灯行为：红→入托盘（进程保持）、黄→最小化到任务栏、绿→最大化/还原切换；双击标题栏 = 绿
- [ ] 标题栏可拖拽移动窗口（`data-tauri-drag-region`），红绿灯按钮不触发拖拽
- [ ] **窗口圆角 8 px + 投影在 Win10 与 Win11 上视觉一致**：无 DWM 双圆角、无白边/黑边、Win11 不弹 Snap Layouts 差异可接受
- [ ] 交付前在 150% DPI 缩放下验证布局（WebView2 随系统缩放）

**系统集成**

- [ ] 红灯 → 最小化到托盘（进程保持）；托盘菜单 Open 恢复窗口、Exit 完全退出；左键点托盘图标恢复窗口
- [ ] 非管理员启动：自动弹 UAC，同意后以管理员重启；拒绝时提示后不启动、无残留窗口/进程
- [ ] 打包产物脱离开发环境可独立运行；`config.json` 写在 exe 同级且便携可携

## 7. 测试策略（两层 E2E，全部本地）

**结论**：满足"本地完整的端到端界面操作调试"。应用要求管理员权限，而 Playwright 没有 Tauri 专用驱动，故采用**两层**互补方案，均在本地一条命令跑通。

### 7.1 浏览器层（主力日常迭代，免提权）

`tests/e2e/e2e.spec.ts` + `tests/playwright.config.ts`（`webServer` 自动起 `npx vite --port 5173`，`reuseExistingServer: true`）。

- 因 `api.ts` 在无 `__TAURI_INTERNALS__` 时自动降级 mock（3.7），**Chromium 里跑的就是与真机同一份 UI 代码**，只是后端为内存 mock
- 已覆盖 18 例：初始渲染、点单选应用/DHCP 重置、Edit-Save 成功、非法 IP 拒绝、重名拒绝、保留名拒绝、Add 进编辑态、Delete 确认/取消、Refresh、切换网卡重取 DNS、右键菜单出现、Copy（无选区/有选区，用 `navigator.clipboard.readText()` 回读断言）、红绿灯存在可点、DHCP 行无 Edit/Delete、地址 `user-select: text`
- 命令：`npm run test:e2e`；迭代快（无 Rust 编译、无 UAC）

### 7.2 真机层（打包 exe + CDP，验收用）

`tests/e2e/exe-e2e.spec.ts`：

- 用 `spawn(EXE_PATH)` 启动 `release/DNS-Switch.exe`，注入 `DNS_SWITCH_ALLOW_NO_ADMIN=1`（跳过 UAC）+ `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9333`，再 `chromium.connectOverCDP("http://127.0.0.1:9333")` attach **真实 WebView2**
- 断言取自**真实系统**：真实网卡列表、真实 Current DNS；写类操作（应用组 / 重置 DHCP）在未提权时按优雅失败分支断言（`fails gracefully`），提权跑则真实改 DNS
- 已覆盖 20 例：窗口标题、红绿灯存在、真实网卡下拉、真实 DNS 显示与切换网卡、Refresh、组行渲染、应用组/DHCP（含未提权优雅失败）、Edit-Save、非法 IP/重名拒绝、Add、Delete 确认/取消、右键菜单、Copy 状态提示、DHCP 行无按钮
- 独立调试脚本 `tests/e2e/exe-cdp-test.mjs`：免 Playwright 直连 CDP 打印 DOM/标题，用于快速定位"是否 attach 成功"

### 7.3 后端与人工项

- **Rust 侧**：`run_ps` 的 JSON 解析（单元素/数组）、非 0 码错误文本、`load/save_config` 原子写与损坏回退、`isValidIPv4`/重名/保留名 —— 以 `cargo test` + 管理员终端直跑真实 PowerShell 回读（等价 `ipconfig /all` 验证）
- **托盘菜单为原生区域，Playwright/CDP 不可达**：**人工**验证 关闭→托盘、Open 恢复、Exit 退出、左键显示
- **UAC 弹窗位于 secure desktop，无法自动化**：提权行为人工验证一次；其余测试在提权终端跑
- **约定**：优先元素状态/DOM 断言而非像素断言（WebView2 受抗锯齿/DPI 影响）；测试依赖在 `devDependencies`；测试代码集中在 `tests/`；无 CI，全部本机

## 8. 构建与出包（全程本地）

**决策**：全程本地开发 + 打包，无 Git CI。工具链一次性安装：Node.js LTS + npm、Rust 工具链（`rustc/cargo`）、Microsoft C++ Build Tools（MSVC）、WebView2 Runtime（Win10/11 默认自带）。

- 图标：`node scripts/generate-icon.mjs`（读 `scripts/icon-source.png`，圆底 + 字母 "D"）→ 生成 `src-tauri/icons/` 全套 PNG + `icon.ico` + `icon.icns`；应用图标与托盘图标同源
- 开发：`npm run tauri:dev`（先跑 Vite dev server，HMR）；纯前端逻辑迭代可用 `npm run dev`（浏览器 mock）
- 打包：`npm run package:win`（= `tauri build`，内部先 `npm run build` 产出 `dist/` 再编译 Rust release 并 bundle）
  - 产物：`src-tauri/target/release/bundle/nsis/DNS Switch_0.1.0_x64-setup.exe`、`bundle/msi/*.msi`，以及 `target/release/DNS-Switch.exe`
  - **便携分发**：已把单 exe 与便携目录拷到 `release/`（`release/DNS-Switch.exe` ≈ 8.9 MB；`release/DNS-Switch-win32-x64/` 内含 exe + 同级 `config.json`）——拷走即用，`config.json` 就在 exe 旁边
- 无代码签名时 SmartScreen 仍可能提示（v1 接受，后续可代码签名缓解）

**迭代节奏**：改前端 → Vite HMR 即改即见；改 Rust → `tauri dev` 自动重编译；调外观 → 浏览器模式（无 Rust 编译）先看；满意后 `npm run package:win` + `npm run test:e2e`（含 exe 层）验收。

## 9. 风险与备注

- **管理员权限**：启动检测 + 自提升，用户拒绝 UAC 则程序不运行——强制管理员权限的必然结果；`DNS_SWITCH_ALLOW_NO_ADMIN` 仅供测试，勿用于正式分发说明
- **WebView2 依赖**：Win10 1803+ / Win11 一般已内置 Runtime；若目标机缺失需安装 Evergreen Runtime（约 1.5 MB 引导器）或改用 Fixed Version 分发
- **PowerShell 冷启动约 0.5~1 s**：切换有短暂延迟（async + "Applying…" 状态缓解）；必要时改 `netsh`（3.1）
- **`run_ps` 目前未设超时**（原计划 15 s 强杀）：极端情况下 PowerShell 挂起会占住该命令线程 —— 列为后续项（用 `WaitWithTimeout` 或子线程 + `mpsc::recv_timeout` 实现）
- **DHCP 判定依赖注册表权限**：读 `HKLM\...\Tcpip\Parameters\Interfaces\{guid}` 需管理员（本应用已强制提权，故成立）；若将来放开非提权模式，该判据会退化
- VPN/虚拟网卡可能出现在下拉（v1 仅按 `Status -eq 'Up'` 过滤，后续可加名称黑名单）
- 仅支持 Windows 平台（PowerShell/cmdlet、`dwmapi`、UAC 均为 Win32 专有；其余平台代码路径未适配）
- **无边框 + 透明窗口风险**（Windows 特有）：① 边缘 resize 命中区需靠 CSS `inset: 24px` 的透明外边距保留拖拽余量，需人工验证边缘可 resize；② 个别显卡驱动下透明区可能闪烁（v1 接受）；③ Alt+Tab 缩略图可能带透明背景（视觉可接受）
- **Win11 Snap Layouts**：无边框窗口 hover 最大化按钮时不弹 Snap Layouts 浮层（无原生标题栏）；已通过 `DWMWA_DONOTROUND` 保证圆角观感一致，浮层差异 v1 接受
- HiDPI：WebView2 随 Windows 缩放原生支持，交付前 150% 验证一次即可
- **无跨线程 UI 约束**：托盘回调、窗口事件均在 Tauri 主事件循环内（Rust 端持有事件循环），命令在工作线程执行并回投结果，无需手工派发
- 迁移遗留待清理（不影响功能）：`tests/e2e/exe-e2e.spec.ts` 注释里仍写 "mirror `electron/dns-core.cjs`"（对应逻辑现在在 `src-tauri/src/commands.rs`）；`src-tauri/target/{debug,release}/`、`.svelte-kit/` 等构建缓存仍在本地（已被 gitignore）；`README.md` 引用的截图路径为 `docs/screenshot.png`（实际文件在根目录 `release-screenshot.png`）

---

## 10. 实现状态速览（当前 vs 计划）

| 模块 | 状态 | 备注 |
|---|---|---|
| DNS 四操作 + DHCP 判定（注册表法） | ✅ 已实现 | `commands.rs` 209 行 |
| 配置持久化（原子写 + 默认回退 + 启动恢复） | ✅ 已实现 | `config.rs` |
| Aqua 外观（标题栏/红绿灯/卡片/glossy/色板变量） | ✅ 已实现 | `TitleBar.svelte` + `theme.css` |
| Win10/11 无边框圆角与投影一致 | ✅ 已实现 | 配置 + CSS + DWM 三重 |
| 地址选择 + 右键 Copy + Ctrl+C | ✅ 已实现 | 右键菜单当前仅 **Copy** 一项（原计划另有 "Copy All"，实现上无选区时 Copy 已覆盖"复制整段"，故未单列） |
| 托盘 Open/Exit + 关闭入托盘 + 左键显示 | ✅ 已实现 | 需人工验收（原生菜单不可自动化） |
| UAC 自提升 + 拒绝退出 + 测试逃生门 | ✅ 已实现 | |
| 浏览器层 E2E（18 例）/ exe 层 E2E（20 例） | ✅ 已实现 | `npm run test:e2e` |
| 打包（NSIS/MSI + 便携 exe） | ✅ 已实现 | `release/` 已就位 |
| `run_ps` 超时强杀（15 s） | ⏳ 未实现 | 见第 9 节风险项 |
| `netsh` 提速备选路径 | ⏳ 未实现 | v1 明确保留 PowerShell |
| VPN/虚拟网卡名称黑名单 | ⏳ 未实现 | v1 仅按 Up 过滤 |
| Rust 单元测试（`cargo test`） | ⏳ 待补 | 现由两层 E2E 覆盖主要路径 |

## 附：迁移说明（Electron → Tauri 2）

| 维度 | Electron（原方案） | Tauri 2（现方案） |
|---|---|---|
| 后端 | `electron/main.cjs` + `preload.cjs`（Node.js） | `src-tauri/src/*.rs`（Rust） |
| 通信 | `ipcRenderer.invoke` ↔ `ipcMain.handle` + `contextBridge` | 前端 `invoke()` ↔ `#[tauri::command]` |
| 渲染 | 自带 Chromium | 系统 WebView2 |
| 体积 | 80–150 MB 运行时 | 约 8.9 MB 单 exe |
| 窗口 | `frame:false` + `transparent:true` + `-webkit-app-region: drag` + `win:close/minimize/maximize` 自定义 IPC | `decorations:false` + `transparent/shadow:false` + `data-tauri-drag-region` + `getCurrentWindow()` 内建 API + `DwmSetWindowAttribute` |
| 托盘 | `new Tray()` + `Menu.buildFromTemplate` + `win.on('close')` | `TrayIconBuilder` + `Menu::with_items` + `on_window_event(CloseRequested)` |
| 提权 | `is-elevated` + `Start-Process -Verb RunAs` | `IsUserAnAdmin` + `ShellExecuteW("runas")` |
| 出包 | `electron-packager` → `release/dns-switch-win32-x64/` | `tauri build` → NSIS/MSI + `release/` 便携 exe |
| E2E | Playwright `_electron.launch()` | ① 浏览器 + `api.ts` mock ② `spawn(exe)` + `connectOverCDP(9333)` |

关键提交（时间正序）：

| SHA | 内容 |
|---|---|
| `122bffe` | 初始提交：Electron + Svelte/Vite 版本 |
| `ccd5ee5` / `2b24f3c` | 地址右键复制 + 修复 Current DNS 分隔处右键无菜单 |
| `ae4018f` | 移除 "Copy All"，Copy 改为"有选区复制选区、无选区复制光标处整段地址" |
| `cd585d1` | **Migrate from Electron to Tauri 2**（Rust 后端 + 脚本/测试切换） |
| `25f537b` | 组切换稳健化 + 类型标注 + CDP 连接修复（exe 层 E2E 可用） |
| `2a3ec27` / `4a6b550` | **DHCP 判定改用注册表 `NameServer` + 用 `InterfaceGuid` 定位接口键**（勿依赖空地址列表） |
| `4c4d552` | 启动强制管理员权限（UAC 自提升）+ CDP 测试环境变量逃生门 |
| `6ba5b3a` | 配置持久化 + 启动恢复 `last_adapter` |
| `b91105f` | 关闭最小化到系统托盘 |
| `5e1c63b` | 新应用图标（浅蓝 3D 圆角 + 白字 D），`scripts/generate-icon.mjs` 生成全套图标 |

### 调研来源

- Tauri 2 官方文档（Window / Tray / Commands / Config）：<https://tauri.app/>
- Tauri `data-tauri-drag-region` / `getCurrentWindow` API：<https://tauri.app/reference/javascript/api/namespacewindow/>
- WebView2 远程调试（`--remote-debugging-port` / `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS`）：<https://learn.microsoft.com/microsoft-edge/developers/remote-debugging-pipe>
- Playwright `connectOverCDP`：<https://playwright.dev/docs/api/class-browsertype#browser-type-connect-over-cdp>
- `DwmSetWindowAttribute` / `DWM_WINDOW_CORNER_PREFERENCE`：<https://learn.microsoft.com/windows/win32/api/dwmapi/nf-dwmapi-dwmsetwindowattribute>
- Mac OS X Aqua 设计语言参考（截图见 `macos/` 目录）：iCal、Address Book 等经典 Panther/Tiger 应用截图
- Web-to-desktop 框架横评（体积/内存/启动实测）：<https://github.com/Elanis/web-to-desktop-framework-comparison>
- Electron vs Tauri 对比：<https://blog.logrocket.com/tauri-electron-comparison-migration-guide/>
