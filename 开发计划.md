# DNS Switch 开发计划

Windows 下按网卡切换 DNS 地址的小工具（全英文界面 + 系统托盘）。**技术栈：Electron（Node.js 后端 + Chromium 渲染 + Svelte 5 / TypeScript 前端）**，界面**完整复刻经典 Mac OS X Aqua 窗口外观**（Panther/Tiger 时期，2003–2007）：无边框 + 自绘金属质感标题栏（糖果球红绿灯按钮）+ 8px 圆角 + 银色渐变标题栏 + 浅灰底白色卡片行 + Aqua 蓝色强调，观感与 Mac OS X 原生窗口一致（详见 3.4）。设计参考：`macos/` 目录下三张经典 iCal / Address Book 截图。

## 1. 目标与功能

- 选择一张网卡，将其 DNS 切换到指定的一组主/备 DNS 地址
- GUI 全英文界面，**完整复刻经典 Mac OS X Aqua 窗口外观**：无边框窗口 + 自绘金属银色标题栏（左上角红黄绿糖果球红绿灯按钮、居中标题）+ 浅灰（#ECECEC）窗口主体 + 白色圆角卡片行；8 px 圆角窗体，整体观感与 Mac OS X Panther/Tiger 原生窗口完全一致（详见 3.4）
- 系统托盘图标：点关闭按钮最小化到托盘；托盘图标右键菜单含 **Open**（打开界面）和 **Exit**（退出程序）两个菜单项

界面布局（自上而下）：

```
┌──────────────────────────────────────────────────┐
│ 🔴 🟡 🟢              DNS Switch                    │  ← 标题栏（金属银渐变）
├──────────────────────────────────────────────────┤
│  Adapter:  [ WLAN                       v ] [Refresh] │
│  Current DNS:  8.8.8.8  /  8.8.4.4              │
├──────────────────────────────────────────────────┤
│  (o) AliDNS  223.5.5.5  223.6.6.6  [Edit] [Del] │
│  ( ) Google DNS  8.8.8.8  8.8.4.4  [Edit] [Del] │
│  ( ) DNSPod  1.12.12.12  1.12.0.0  [Edit] [Del] │
│  ( ) Automatic (DHCP)  Automatic  —             │
├──────────────────────────────────────────────────┤
│  [ Add ]                                        │
│  Status: DNS on "WLAN" updated successfully.    │
└──────────────────────────────────────────────────┘
```

交互规则：

- **启动恢复上次网卡**：界面打开时优先选中上次切换/选择的网卡（`last_adapter`），而不是默认选第一个；若该网卡已不存在或状态非 Up，回退选中列表第一个网卡；每次切换网卡后将所选写回 `last_adapter`
- **Current DNS 显示行**：位于网卡选择与 DNS 组列表之间，展示当前选中网卡的当前主/备 DNS 地址；**切换网卡时自动触发重新获取**（点 Refresh 时一并刷新）；获取中显示 "Loading..."，若为自动/DHCP 或无地址则显示 "Automatic (DHCP)"
- 每行 = 一个 DNS 组：左侧单选框 + 组名 + 主 DNS + 备 DNS + 右侧 **Edit/Save 切换按钮**，其右边紧跟 **Delete 按钮**
- **点选单选框即生效**：立即把该组的主/备 DNS 应用到上方选中的网卡，状态栏提示结果；应用成功后同步刷新 Current DNS 显示行
- **单选选中态反映网卡真实当前 DNS**：加载/切换网卡时按该网卡**实际当前 DNS** 决定勾选——为自动/DHCP 时勾选 Automatic (DHCP) 行；当前主/备 DNS 与某组完全一致时勾选该组；**若不属于本工具 DNS 列表（如在本工具之外被手动改过），所有单选框保持全不选**（此时不自动应用任何组）
- 点 **Edit**：该行的**组名**和**主/备地址**均变为可编辑输入框，按钮变为 **Save**；Save 时校验 IP 合法性与**组名唯一性**（不能为空、不能与其他组重复），全部通过才写回配置并恢复只读，未通过则状态栏提示且不保存
- **Add**：新增一组 DNS（组名可编辑，主/备默认空），加入列表
- **Delete**：每个 DNS 组行右侧（Edit 按钮右边）各有一个 Delete 按钮，点击删除**该组**；弹框二次确认（`Yes/No`）后才删除；Automatic (DHCP) 行没有 Delete 按钮
- **Automatic (DHCP) 固定行**：列表末尾恒定追加一行 "Automatic (DHCP)"（不写入配置）；点选它即把当前网卡恢复为 DHCP 自动获取；该行**既无 Edit 也无 Delete 按钮（不可编辑、不可删除）**；组名 "Automatic (DHCP)" 为保留名，Add/Edit 时重名被拒绝
- **DNS 地址选择与复制**：界面展示的所有 DNS 地址（**Current DNS 行**、**每组行的主/备地址**）均支持**鼠标拖拽选择**（可整段选中或只选其中一部分）；在地址上**右键**弹出上下文菜单，含 **Copy**（复制当前选中部分；无选中时复制整段地址）与 **Copy All**（复制整段地址），复制结果写入系统剪贴板；**Ctrl+C** 亦可复制选中部分（实现见 3.4）
- 点窗口**红灯（关闭）**按钮 **不退出**，最小化到托盘；托盘右键 **Open** 恢复窗口，**Exit** 真正退出
- **绿灯**最大化/还原窗口；**黄灯**最小化到任务栏；双击标题栏 = 绿灯功能

## 2. 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 框架 | **Electron**（Node.js 后端 + Chromium 渲染前端） | 成熟的跨平台桌面框架；前端即 HTML/CSS，**圆角/投影/字体等 Mac OS X Aqua 观感用 CSS 直接实现**；Chromium 对 CSS 渐变、box-shadow、border-radius 支持完整；MIT 许可；生态丰富（electron-packager 出包、Playwright E2E） |
| 前端 | **Vite + Svelte 5 + TypeScript** | 编译型框架、无虚拟 DOM、运行时仅几 KB；响应式状态天然适配"行内 Edit/Save 切换"；Vite HMR 让 UI 改动即改即见 |
| 托盘 | **Electron Tray API**（`electron.Tray` + `Menu`） | 官方一等支持，原生菜单；与窗口事件（close → hide）在同一主进程事件循环内联动，无跨线程 UI 问题 |
| 提权 | **Electron 启动时检测管理员权限**（`is-elevated` 或自实现） | 启动时检测当前进程是否具有管理员权限；无权限时通过 `powershell -c Start-Process -Verb RunAs` 重新以管理员身份启动自身；开发态从管理员终端跑 `npm start` 即可（见 3.2） |
| DNS 操作 | Node.js `child_process.exec` 调系统内置 **PowerShell**（`NetAdapter` 模块，备选 `netsh`） | 系统自带、无需第三方驱动，JSON 输出最易解析（见 3.1） |
| 配置存储 | 本地 **JSON** 文件（exe 同级，`JSON.stringify`/`JSON.parse`） | 简单、可手改、便于便携 |
| 构建/出包 | **`electron-packager`**（`npm run package:win`，出 Windows 便携目录） | 一条命令出包；产物脱离开发环境可独立运行；图标用 PNG 源图自动生成 |
| 测试 | **Playwright（E2E，主力）+ Svelte 组件快测** | Playwright 原生支持 Electron 驱动，本地无需外部驱动即可驱动**真实窗口**（点击→Node.js 后端真跑 PowerShell→UI 状态断言）；Svelte 组件测试快速迭代前端逻辑（见第 7 节） |

依赖汇总：运行期 Electron 运行时（含 Node.js + Chromium，约 80–150 MB）；前端构建期仅 Vite/Svelte/TypeScript 工具链；Node.js 侧仅使用 `child_process`、`fs`、`path` 等内置模块，无第三方运行时依赖。

> 备选对比（基于 2025–2026 年轻量级 Windows GUI 框架调研，来源见文末）：
> - **Tauri 2（Rust + WebView2）**：单 exe 仅 5–10 MB，轻量优秀；但本项目需要 Electron 的 `Tray`、`BrowserWindow`、IPC 等成熟 API 及 Playwright 完整 Electron 驱动支持。
> - **Wails v3（Go + WebView2）**：同为 WebView 混合方案、体积相当，托盘是 v3 头等特性；但 v3 尚处 **Beta**，且存在自 alpha.22 起 `Hidden: true` 窗口选项在 Windows 失效的已知 bug（wails #4498，与"隐藏到托盘"场景直接相关）；E2E 无官方 WebDriver 方案。风险高于 Electron。
> - **egui / eframe（Rust 纯原生）**：单文件 5–15 MB、启动极快；但**无内置托盘**（需自行集成 `tray-icon` 并手工处理事件循环，egui #737/#7401），immediate-mode 下 Mac OS X Aqua 风格卡片/文本选择复制需大量自绘，且无标准 E2E 通道——"本地完整端到端界面调试"最难满足。
> - **Avalonia 11（C#/.NET）**：表现力强，但自包含发布 50–100+ MB；托盘靠第三方（H.NotifyIcon）；E2E 需自组 UIA/FlaUI 方案。
> - **Flutter Desktop**：UI 现代但引擎 60 MB+，Material 风格与 Mac OS X Aqua 观感路线不符，托盘与 E2E 均靠社区包。
> - **WinUI 3 / Windows App SDK**：原生观感最佳，但打包（MSIX）与自包含分发繁琐，托盘/E2E 生态同样不全。
>
> 综上，**Electron** 在"Mac OS X Aqua 风格 CSS 自由度 + 原生托盘 + Playwright 官方 E2E 支持 + 成熟 IPC 模型"上综合最优，且 Playwright 原生支持 Electron 驱动——直接满足"本地完整的端到端界面操作调试"要求。

## 3. 核心实现要点

### 3.0 工程结构（Electron 主进程 + Chromium 渲染进程）

- **主进程 + 渲染进程模型**：Electron 主进程（Node.js）持有事件循环、托盘、窗口与全部 IPC handler；Chromium 渲染 Svelte 前端
- **前后端通信**：前端 `ipcRenderer.invoke("cmd", args)` → 主进程 `ipcMain.handle` 命令函数（`async` + 错误回传）；DNS 子进程在命令内后台执行，**天然不阻塞 UI**——前端 `await` 后直接更新状态栏/单选态即可
- 窗口：标题 "DNS Switch"，初始 580×480，居中；**`frame: false`（无边框）+ `transparent: true`**，由前端自绘 Mac OS X Aqua 风格标题栏（含红绿灯按钮、拖拽区、标题居中）；详见 3.4
- **本地调试闭环**：`npm run dev`（Vite dev server）+ `npm start`（Electron）启动真实窗口 + HMR（前端改动即改即见）；Chromium DevTools（`webContents.openDevTools()`）调试前端；再叠加第 7 节 Playwright E2E，覆盖"本地完整端到端界面操作调试"

```js
// electron/main.cjs —— IPC 命令层示意
ipcMain.handle("set_dns", async (event, adapterName, servers) => {
  const result = await runPowerShell(`Set-DnsClientServerAddress ...`);
  if (result.exitCode !== 0) throw new Error(result.stderr);
});
```

```ts
// src/api.ts —— 前端调用
export const setDns = (adapter: string, servers: string[]) =>
  (window as any).electronAPI.setDns(adapter, servers);
```

```js
// electron/preload.cjs —— contextBridge
contextBridge.exposeInMainWorld("electronAPI", {
  setDns: (adapter, servers) => ipcRenderer.invoke("set_dns", adapter, servers),
  // ...
});
```

### 3.1 DNS 操作（`electron/dns-core.cjs`，`child_process.exec`）

统一封装 `runPs(cmd) -> Promise<{stdout, stderr, exitCode}>`：`powershell -NoProfile -Command <cmd>`，**15 s 超时强杀**；非 0 返回码 / 超时 / UTF-8 解析失败 → 抛 Error（带 stderr 原文），由前端捕获显示在状态栏。子进程一律加 `windowsHide: true` 标志，避免窗口化应用闪黑框。

| 功能 | 命令 |
|---|---|
| 列出可用网卡 | `Get-NetAdapter \| Where-Object Status -eq 'Up' \| Select Name,InterfaceIndex \| ConvertTo-Json` |
| 读取网卡当前 DNS | `Get-DnsClientServerAddress -InterfaceIndex <n> -AddressFamily IPv4 \| ConvertTo-Json` |
| 设置 DNS | `Set-DnsClientServerAddress -InterfaceIndex <n> -ServerAddresses ('<primary>','<secondary>')` |
| 恢复自动(DHCP) | `Set-DnsClientServerAddress -InterfaceIndex <n> -Reset` |

- 只列出 `Status -eq 'Up'` 的网卡，过滤循环/已禁用网卡；后续可再加过滤 VPN/虚拟网卡
- PowerShell JSON 输出用 `JSON.parse` 解析；注意单元素时 `ConvertTo-Json` 不包数组，统一规范化处理
- **提速备选**：`netsh interface ip set dns "WLAN" static 8.8.8.8 primary` + `netsh interface ip add dns "WLAN" 8.8.4.4 index=2`、`set dns ... dhcp`、`netsh interface ip show dns "WLAN"`；无 PowerShell 冷启动（约 0.5~1 s），但输出需手工解析。v1 先保 PowerShell 求稳，留 netsh 作可选项

### 3.2 管理员权限（启动时检测 + 自提升）

修改网卡 DNS 必须管理员权限：

- **启动检测**：`electron/main.cjs` 启动时通过 `is-elevated` 包（或自实现 `net session` 命令）检测当前进程是否具有管理员权限
  - 已提权 → 正常启动窗口
  - 未提权 → 通过 `powershell -c Start-Process -Verb RunAs -ArgumentList '\"<path to exe>\"'` 重新以管理员身份启动自身，原进程退出
- **开发态**：从**管理员终端**运行 `npm start`（终端提权一次，子进程全部继承，无需自我提权代码）；拒绝提权时进程不启动、无残留

### 3.3 系统托盘（Electron Tray API）

- 图标：准备一张 256×256 PNG（圆底 + 字母 "D"）作为 `app-icon.png`，Electron 自动缩放至各尺寸
- `electron/main.cjs`：`new Tray(iconPath)` + `Menu.buildFromTemplate([{label: 'Open', click: () => showWindow()}, {label: 'Exit', click: () => app.quit()}])`；`tray.on('click')` / `tray.on('right-click')` 分发
- **Open** → `mainWindow.show() + mainWindow.focus()`；**Exit** → `app.quit()`（托盘图标随进程销毁自动移除）
- **关闭→入托盘**：`mainWindow.on('close', e => { e.preventDefault(); mainWindow.hide(); })`；此后窗口常驻隐藏，靠托盘 Open/Exit 控制
- 可选：首次隐藏时托盘气泡提示 "Running in tray"（`tray.setToolTip` + Notification API 可后续加）
- 托盘菜单回调运行在主进程事件循环内，直接操作 BrowserWindow/app 即可，**无跨线程派发问题**

### 3.4 GUI（Svelte + CSS，Mac OS X Aqua 复刻）

- 主题基调：**固定浅色**（CSS 即真理，无系统深色跟随问题）
- 设计基调（**经典 Mac OS X Aqua 观感，Panther/Tiger 时期 2003–2007**，参考 `macos/` 目录下 iCal / Address Book 截图）：
  - 配色（经典 Aqua 色板，集中为 `theme.css` 的 `:root` CSS 变量，所有组件引用同一组色值）：窗口底色 `#ECECEC`（Aqua 浅灰）、卡片/行底色纯白 `#FFFFFF`、主文字 `#1D1D1F`、次级文字 `#5F5F64`、边框/分隔线 `#C9C9C9`；强调色取 Aqua 蓝 `#3876D6`（主按钮/选中态），成功绿 `#28C840`、失败红 `#FF3B30`、进行中灰 `#8E8E93`
  - 圆角与投影（对标 Aqua 卡片风格）：卡片 `border-radius: 6px` + 1px 浅灰描边 + **柔和投影 `box-shadow: 0 1px 2px rgba(0,0,0,.12)`**；按钮/输入框/下拉 `border-radius: 5px`；窗口整体 `border-radius: 8px`（Aqua 窗口四角圆角）
  - 字体：`font-family: "Lucida Grande", -apple-system, "Segoe UI", "Helvetica Neue", "Microsoft YaHei", sans-serif`——Aqua 系统字体为 Lucida Grande，Windows 回退至 Segoe UI；字号层级：标题 13px/600、正文 13px、辅助 11px，行距舒适
  - 强调/状态色：主按钮（Add/Refresh）用 Aqua 蓝渐变（`#9CC7FF→#5B9BE8→#3876D6→#1E5FC0` 四段 glossy）；次要按钮白→浅灰 two-tone 渐变；状态栏成功绿/失败红/进行中灰按上述色板（class 切换）；悬停过渡 `transition: all .1s ease`
  - **Mac OS X Aqua 风格自绘标题栏（无边框窗口）**：Electron `BrowserWindow` 设 `frame: false, transparent: true`；外层 `#root` 设 `border-radius: 8px; overflow: hidden`（对齐 Aqua 窗口圆角）；`body` 背景透明使窗口外区域透明
  - **红绿灯三按钮**（标题栏左上角，左→右：关闭 / 最小化 / 最大化）：
    - 直径 **13 px**，间距 **8 px**，距左边缘 **13 px**，垂直居中于 32 px 高的标题栏
    - **聚焦态颜色**（Aqua 糖果球质感，径向渐变 + inset 高光）：
      - 关闭（红）`radial-gradient(circle at 50% 38%, #FF9D94, #FF5F56 52%, #E5433A)`，hover 符号 `×`（`#5A0C08`）
      - 最小化（黄）`radial-gradient(circle at 50% 38%, #FFE28C, #FFBD2E 52%, #E6A01F)`，hover 符号 `−`（`#78540C`）
      - 最大化/还原（绿）`radial-gradient(circle at 50% 38%, #8FE9A0, #28C840 52%, #1EAE35)`，hover 符号 `⊞`（`#185C1E`）
    - **失焦态**：三按钮统一灰色，hover 才显示符号
    - 实现：三个 `<button>` 含 SVG 图标（opacity 0→1 on group hover）；外层 `.aqua-traffic-lights` 设 `:hover` 同时显示三个符号（macOS 行为：hover 组内任意一个即显示全部符号）
  - **按钮行为映射**（前端 JS + IPC）：
    - 红 → `ipcRenderer.invoke('win:close')`（入托盘，保持进程）
    - 黄 → `ipcRenderer.invoke('win:minimize')`
    - 绿 → `ipcRenderer.invoke('win:maximize')`
  - **标题文字**：水平居中，`font-size: 13px; font-weight: 600; color: #4D4D4D`（Lucida Grande / Segoe UI 回退）
  - **标题栏背景**：金属银纵向渐变 `linear-gradient(180deg, #FBFBFB, #F1F1F1 45%, #E7E7E7 60%, #D9D9D9)` + 顶部 `inset 0 1px 0 rgba(255,255,255,.95)` 高光 + 底部 1px `#9D9D9D` 分隔线
  - **拖拽**：标题栏 `div` 设 `-webkit-app-region: drag`，红绿灯按钮设 `-webkit-app-region: no-drag` 防止误触拖拽
  - **双击标题栏**：toggle maximize（Aqua zoom 行为），监听 `dblclick` → `win:maximize` IPC
  - **窗口圆角**：`#root { border-radius: 8px; overflow: hidden }`；`transparent: true` 使窗口外区域透明，模拟 Aqua 四角圆角
- 布局与交互同第 1 节：顶部网卡下拉（Aqua 风格 `<select>`：白底 + 右侧蓝色渐变 chevron 区域）+ Refresh 按钮（Aqua two-tone）；中部 "Current DNS" 行 + 动态 DNS 组行（Aqua radio/组名/主备地址/Edit-Save/Delete，Delete 位于 Edit 右边）+ 末尾固定 Automatic (DHCP) 行（无 Edit/Delete）；**底部工具栏条**（Aqua 风格：浅灰渐变背景 `#F4F4F4→#D8D8D8` + 顶部 1px 分隔线 + 矩形小圆角按钮，模拟 iCal 截图中的 bottom bar）含 Add + 状态栏
- **各列左对齐**：CSS Grid 固定列模板（单选/组名/主 DNS/备 DNS/Edit/Delete 各占一列）；组名列固定宽度，组名长短不一时主/备/按钮列仍纵向对齐；组多时外层套 `overflow-y: auto` 滚动容器（Aqua 风格滚动条：11px 宽、圆角灰色渐变 thumb）
- **Edit 态**：Svelte 响应式——每行持 `editing` 状态，为真时组名/主/备渲染 `<input>`（Aqua 内凹阴影输入框），否则渲染只读文本；按钮文案切 Save（Aqua 蓝色 glossy 默认按钮）；Save 校验通过才写回配置并退出编辑态
- **单选勾选逻辑**：切换/加载网卡时先读实际当前 DNS 再决定勾选（DHCP→Automatic 行；与某组主/备完全一致→该组；不在列表内→全不选），规则同第 1 节
- **启动恢复上次网卡**：网卡列表加载完成后，若 `last_adapter` 仍在列表中则选中它，否则选第一个 Up 网卡；随后按上述单选逻辑获取该网卡当前 DNS 决定勾选
- 删除二次确认用自绘 Aqua 风格确认对话框（浅灰渐变背景 + 10px 圆角 + 柔和投影 + No/Yes 两按钮，Yes 为 Aqua 蓝色默认按钮）
- **DNS 地址选择与复制**（Chromium 下为原生能力）：
  - 地址单元格（Current DNS 行主/备、每组行主/备地址）用 `<span class="addr">` + CSS `user-select: text`：**鼠标拖拽选择、Ctrl+C 复制选区均为浏览器原生行为**；单元格本身非输入框，天然"只读不可键入"
  - **右键菜单**：监听 `contextmenu` 事件，弹自绘 Aqua 风格小菜单（Copy / Copy All 两项，白→浅灰渐变背景 + 圆角 + 深投影；hover 项为 Aqua 蓝渐变 + 白字）；Copy 取 `window.getSelection().toString()`，无选区则复制整段地址；经 **`navigator.clipboard.writeText()`** 写入系统剪贴板，状态栏提示 "Copied <ip>"
  - Automatic 行的 "Automatic (DHCP)" 占位非真实 DNS 地址，`user-select: none`（不可选择）

### 3.5 数据模型与持久化（`config.json`，exe 同级）

```json
{
  "dns_groups": [
    {"name": "AliDNS",     "primary": "223.5.5.5", "secondary": "223.6.6.6"},
    {"name": "Google DNS", "primary": "8.8.8.8",   "secondary": "8.8.4.4"}
  ],
  "last_adapter": "WLAN",
  "selected_group": "AliDNS"
}
```

- 位置：打包后取 exe 所在目录（便携、易手改）；开发态落在项目根目录；Node.js 侧 `JSON.parse`/`JSON.stringify` + `load_config()/save_config()` 两个 IPC 命令；加载失败/损坏 → 回退到上述默认数据
- `selected_group` 记录上次勾选的组名，可为保留值 `"Automatic (DHCP)"`，**也可为空字符串（表示全不选）**；加载/切换网卡时勾选态**不以此值为准，按网卡实际当前 DNS 重新计算**（当前 DNS 不在列表内则为空/全不选）
- `last_adapter` 记录上次选中/切换的网卡名，**每次切换网卡即写回**；启动时用于恢复上次网卡（见 3.4），对应网卡已消失/非 Up 时忽略该值
- 默认预置 3 组常用 DNS（AliDNS / Google / DNSPod）；写盘原子化（先写临时文件再 rename 覆盖，防写坏）

### 3.6 校验与异常

- IPv4 用 `isValidIPv4(ip)` 函数校验（前端即时提示 + 后端验证双保险）；组名校验统一应用于 **Edit→Save** 与 **Add** 两入口：trim 后非空且与其他组名不重复（大小写不敏感），重复则拒绝并状态栏提示
- 删除后若选中项被删 → 自动选中第一组（列表为空则清空选中）
- DNS 切换为 async 命令（不阻塞渲染），状态栏 "Applying..." → 结果；失败红色提示且**不改变单选状态**
- PowerShell 输出按 UTF-8 解析；`Set-DnsClientServerAddress` 失败（如网卡被占用/权限不足）时把 stderr 原文带入错误提示

## 4. 代码结构

```
dns_switch/
├── package.json            # 构建脚本（dev/build/start/package:win）+ electron / electron-packager
├── vite.config.ts
├── electron/
│   ├── main.cjs            # Electron 主进程：窗口、托盘、IPC handlers、提权检测
│   └── preload.cjs         # contextBridge：暴露 electronAPI（window 控制 + DNS 操作 + 配置读写）
├── src/                    # 前端（Svelte 5 + TypeScript）
│   ├── main.ts             # 挂载入口
│   ├── App.svelte          # 主界面：Aqua 卡片布局、动态组行、Current DNS 行、底部工具栏
│   ├── components/         # DnsRow / ContextMenu / ConfirmDialog / TitleBar（Aqua 金属银标题栏）等
│   ├── api.ts              # IPC 封装：listAdapters/getCurrentDns/setDns/resetDnsToDhcp/loadConfig/saveConfig
│   ├── types.ts            # TypeScript 类型定义
│   └── theme.css           # Mac OS X Aqua 色板（:root 变量）/金属银标题栏/糖果球红绿灯/glossy 按钮/圆角/字体
├── index.html              # Vite 入口 HTML
├── dist/                   # 构建产物（vite build 输出）
├── tests/                  # Playwright E2E（第 7 节）
└── config.json             # 运行期生成（exe 同级；开发态在项目根目录）
```

## 5. 开发步骤

1. **工具链**（一次性）：安装 Node.js LTS + npm；`npm create vite@latest`（Svelte-TypeScript 模板）；`npm install electron electron-packager --save-dev`；管理员终端 `npm start` 跑通空窗口（580×480 居中、Aqua 风格）
2. **electron/dns-core.cjs**：实现 4 个 PowerShell 操作（`windowsHide: true` + 15 s 超时 + 错误捕获 + `JSON.parse` 解析含单元素规范化），Node.js 测试脚本自测解析与错误分支
3. **配置持久化**：`config.json` JSON 读写 + 默认数据 + 损坏回退 + 原子写（临时文件 + rename）
4. **Mac OS X Aqua 风格标题栏**：`main.cjs` 设 `frame: false, transparent: true`；实现 `TitleBar.svelte` 组件——左侧 3 个 13px 红绿灯糖果球按钮（红 `#FF5F56`/黄 `#FFBD2E`/绿 `#28C840`，8px 间距，Aqua 径向渐变 + inset 高光），hover 显示符号（×/−/⊞）；标题 "DNS Switch" 居中（13px/600/`#4D4D4D`）；标题栏金属银渐变 `#FBFBFB→#F1F1F1→#E7E7E7→#D9D9D9` + 底部 1px `#9D9D9D` 分隔线；`-webkit-app-region: drag` 拖拽 + 双击 toggleMaximize；窗口 `#root` 设 `border-radius: 8px; overflow: hidden`；失焦时三按钮变灰
5. **UI 骨架**：顶部网卡下拉（Aqua 风格 select）+ Refresh；Current DNS 行；中部动态 DNS 组行（Aqua radio/组名/主备地址/Edit-Save/Delete）+ 固定 Automatic (DHCP) 行；底部 Aqua 工具栏条（Add + 状态栏）；套用 3.4 Aqua 卡片样式与列对齐（Svelte 组件 + CSS Grid）
6. **地址选择与复制**：`user-select` 可选中地址单元格 + `contextmenu` 自绘 Aqua 风格菜单（Copy/Copy All）+ `navigator.clipboard.writeText()` 写剪贴板；手工验证拖拽选择、右键复制、Ctrl+C
7. **交互逻辑**：IPC 命令打通 DNS 操作；单选即切换（"Applying..." → 结果）、Edit/Save 编辑态、Add 新增、Delete 二次确认（自绘 Aqua 对话框）、IP 与重名校验；用 Playwright 快速驱动前端逻辑
8. **托盘 + 提权**：`electron.Tray` Open/Exit；`close → preventDefault + hide`；启动时管理员权限检测与自提升
9. **联调与打磨**：按第 6 节清单测试，统一英文文案、间距、Aqua 配色与圆角观感；管理员终端联调真实 DNS 切换
10. **测试**：按第 7 节实现 `tests/`（Playwright E2E），第 6 节可自动化项全部通过
11. **打包**：`npm run package:win`（electron-packager）出 Windows 便携目录；便携分发直接取 `release/` 下产物（见第 8 节）

## 6. 验收清单

- [ ] 网卡下拉列出所有 Up 状态网卡，Refresh 可刷新
- [ ] 启动时自动恢复上次选中的网卡（`last_adapter`）；切换后重启仍恢复；该网卡不存在或非 Up 时回退到列表第一个
- [ ] 切换网卡后 Current DNS 行自动重新获取并显示该网卡当前主/备 DNS（DHCP 时显示 "Automatic (DHCP)"）
- [ ] 网卡当前 DNS 不在本工具 DNS 列表（如外部手动改为列表外地址）时所有单选框全不选；与某组主/备完全一致时勾选该组；为 DHCP 时勾选 Automatic (DHCP) 行
- [ ] DNS 列表各列（单选/组名/主/备/Edit/Delete）均左对齐，组名长短不一时后续列仍纵向对齐
- [ ] 点选任一 DNS 组后，`ipconfig /all` 验证该网卡 DNS 已变更为主/备地址，Current DNS 行同步更新
- [ ] 界面任一 DNS 地址（Current DNS 行、各组行主/备地址）均可鼠标拖拽部分选中；且该单元格不可键入（非输入框）
- [ ] 地址上右键出现 Copy/Copy All 菜单；选中后 **Copy** 把选中部分写入系统剪贴板（可粘贴到记事本核对内容一致）；未选中时 Copy 复制整段地址；**Ctrl+C** 亦可复制选中部分
- [ ] Edit → 改地址 → Save：校验生效、持久化到 config.json、重启后保留
- [ ] Add 新增组成功；Edit 可修改组名与地址；组名重复（Edit/Save、Add）或非法 IP 被拒绝并有提示
- [ ] 每行 Delete 按钮（位于 Edit 右边）弹二次确认，确认后该组被删除且配置同步
- [ ] 列表末尾恒有 Automatic (DHCP) 行：无 Edit/Delete 按钮；选中它后 `ipconfig /all` 验证网卡恢复 DHCP；Add/Edit 使用保留名 "Automatic (DHCP)" 被拒绝
- [ ] 红灯按钮 → 最小化到托盘（进程保持）；托盘右键 Open 恢复窗口、Exit 完全退出
- [ ] 非管理员启动：自动弹 UAC，同意后以管理员运行；拒绝时程序直接不启动、无残留窗口
- [ ] config.json 损坏/缺失时程序不崩溃，使用默认数据
- [ ] 界面为全英文、**完整 Mac OS X Aqua 风格窗口**：无边框 + 自绘金属银标题栏（左上角红绿灯 13px 糖果球 ×3，Aqua 径向渐变质感，hover 显示 ×/−/⊞ 符号，失焦变灰）；标题居中 13px 600 `#4D4D4D`；标题栏金属银渐变背景 + 1px 底部分隔线；窗口 `border-radius: 8px` 圆角；浅灰底（`#ECECEC`）+ 白色卡片（`border-radius: 6px` + 柔和 `box-shadow`）、Aqua 蓝强调色（`#3876D6`）/状态色（绿 `#28C840`/红 `#FF3B30`）/柔和悬停过渡；配色取自统一 Aqua 色板（`theme.css`），无深色残留、不随系统切深色
- [ ] 红绿灯按钮行为正确：红→最小化到托盘（进程保持）、黄→最小化到任务栏、绿→最大化/还原切换
- [ ] 标题栏可拖拽移动窗口；双击标题栏触发最大化/还原
- [ ] 窗口四角圆角（8px）视觉正确，无白边/黑边残留
- [ ] 打包后产物脱离开发环境可独立运行，带管理员权限
- [ ] E2E 测试通过：Playwright E2E（`tests/`）全绿
- [ ] 交付前在 150% DPI 缩放下验证一次布局（Chromium 原生随系统缩放）

## 7. 测试策略（E2E 界面测试）

**结论**：支持端到端界面测试。Playwright 原生支持 Electron 驱动（`electron` + `playwright` 集成），本地无需安装任何外部驱动即可驱动**真实窗口**；另有纯浏览器模式快速测前端逻辑。两层均本地运行，覆盖"本地完整的端到端界面操作调试"。

### 7.1 Node.js 单元测试

- `dns-core`：PowerShell JSON 解析（含单元素规范化）、超时/非 0 返回码 → Error 文本
- `config`：读写、默认数据、损坏回退、原子写
- 校验函数：IPv4 解析、组名唯一/保留名拒绝
- 真 PowerShell 端到端可直跑（管理员终端 `node test/dns.test.js`）：`set_dns` 后 `get_dns` 回读，等价 `ipconfig /all` 验证

### 7.2 前端快测（纯浏览器 + Vite，免提权）

- Vite dev server + 普通 Chrome：前端在浏览器跑，IPC 调用被 mock 可**模拟返回值并断言调用参数**（如"点单选 → 断言 set_dns 收到正确参数"）
- 覆盖纯前端逻辑：勾选态计算（含"列表外 DNS 全不选"）、Edit/Save 编辑态切换、Add 行为、IP/重名校验拒绝提示、状态栏文案
- 迭代快：无 Electron 启动、无 UAC，日常开发主力

### 7.3 真机 E2E（Playwright + Electron，主力验收）

- 配置 Playwright `_electron` API 启动 Electron 应用；`page.evaluate()` 执行 IPC mock（构造 DNS 操作失败等分支）
- 真实链路：真实窗口内**点击单选 → Node.js 后端真跑 PowerShell 改 DNS → Current DNS 行刷新 → 断言 UI 状态**；剪贴板断言用 `navigator.clipboard.readText()` 回读（验证 Copy/Copy All/Ctrl+C）
- 覆盖第 6 节绝大多数逻辑项：勾选态计算、Edit/Add/Delete 全流程、配置持久化、启动恢复上次网卡、地址选择复制
- **注意**：应用要求管理员权限，Playwright 需从**管理员终端**运行（终端已提权则子进程直接继承，不重复弹 UAC）
- 示例骨架：

```ts
// tests/dns.spec.ts
import { _electron as electron } from 'playwright';

test('selecting a group applies its DNS', async () => {
  const app = await electron.launch({ args: ['.'] });
  const window = await app.firstWindow();
  await window.locator('[data-testid="adapter-select"]').selectOption('WLAN');
  await window.locator('[data-testid="radio-AliDNS"]').click();
  await window.waitForFunction(() =>
    document.querySelector('[data-testid="status"]')?.textContent?.includes('updated successfully')
  );
  expect(await window.locator('[data-testid="current-dns"]').textContent()).toContain('223.5.5.5');
  await app.close();
});
```

### 7.4 托盘与 UAC（人工验证）

- 托盘菜单为原生区域，Playwright 不可达：**人工**验证 关闭→托盘、Open 恢复、Exit 退出、图标随进程销毁消失
- **UAC 弹窗位于 secure desktop，无法自动化**：提权行为人工验证一次；其余测试以管理员身份运行

### 7.5 边界与约定

- Playwright 自带 `screenshot()` 截图留档，可做粗粒度视觉回归；Chromium 渲染受抗锯齿/DPI 影响，优先**元素状态/DOM 断言**而非像素断言
- 测试依赖独立于运行依赖（`package.json` devDependencies）；测试代码在 `tests/`
- 全部本机运行（无 CI），无 headless 问题

## 8. 构建与出包（全程本地）

**决策**：全程本地开发 + 打包——工具链一次性安装（Node.js LTS + npm），无 Git CI。

- 图标：准备一张 PNG 源图（圆底 + 字母 "D"），`electron-packager` 自动处理
- 打包：`npm run package:win`（electron-packager）：
  - 默认产物：`release/` 目录下含 `dns-switch-win32-x64/` 便携目录
  - **便携单目录**：直接拷走 `release/dns-switch-win32-x64/` 即可运行，无需额外运行时
- 资源/配置：图标等打进产物目录；config.json 写在 exe 所在目录（便携友好）
- 无代码签名时 SmartScreen 可能提示（可后续代码签名缓解）

**迭代**：改前端 → Vite HMR 即改即见；改主进程 → `npm start` 重启 Electron；满意后 `npm run package:win` 出包；全流程本地完成。

## 9. 风险与备注

- 管理员权限：启动检测 + 自提升，用户拒绝则程序无法运行——强制管理员权限的必然结果
- **提权 + Chromium 用户数据目录**：Windows 11 "Administrator Protection" 开启时，提权进程下 Chromium 用户数据目录可能指向错误用户；**缓解**：`main.cjs` 启动时设置 `app.setPath('userData', path.join(exeDir, 'userData'))` 将数据目录固定在 exe 同级
- 工具链门槛：首次需安装 Node.js LTS（一次性）；换来的是完整 Chromium CSS 支持（Aqua 渐变/投影/圆角原生渲染）、官方 E2E 方案
- PowerShell 冷启动约 0.5~1 s，切换有短暂延迟（async 命令 + "Applying..." 状态缓解；必要时切 `netsh`）
- 目标机器需 Electron 运行时（自带 Chromium，无需额外安装）
- VPN/虚拟网卡可能出现在下拉列表（v1 仅按 Up 过滤，后续可加名称黑名单）
- 仅支持 Windows 平台
- 无签名单 exe 分发可能触发 SmartScreen 提示（可后续代码签名缓解，v1 接受）
- HiDPI：Chromium 随 Windows 缩放原生支持，交付前 150% 验证一次即可
- 前端零跨线程 UI 约束：托盘/IPC 回调均在主进程事件循环内（Node.js 单线程），无跨线程 UI 问题
- **无边框 + 透明窗口风险**（Windows 特有）：`frame: false` + `transparent: true` 在 Windows 上可能导致：① 窗口边缘 resize 区域缩小（需验证鼠标在窗口边缘仍可拖拽调整大小）；② 部分显卡驱动下透明区域渲染闪烁（v1 接受）；③ 任务栏 Alt+Tab 缩略图可能显示透明背景（不影响功能，视觉可接受）
- **Win11 Snap Layouts 兼容**：无边框窗口 hover 最大化按钮时，Win11 不会弹出 Snap Layouts 浮层（因无原生标题栏）；v1 接受此差异

---

### 调研来源（2025–2026）

- Electron 官方文档（BrowserWindow / Tray / IPC）：<https://www.electronjs.org/docs>
- Playwright Electron 测试指南：<https://playwright.dev/docs/electron>
- Mac OS X Aqua 设计语言参考（截图见 `macos/` 目录）：iCal、Address Book 等经典 Panther/Tiger 应用截图
- Web-to-desktop 框架横评（体积/内存/启动实测）：<https://github.com/Elanis/web-to-desktop-framework-comparison>
- Electron vs Tauri 对比：<https://blog.logrocket.com/tauri-electron-comparison-migration-guide/>
